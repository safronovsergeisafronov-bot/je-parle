# AI Design Prompt Guide — Vibe-Coding Edition

> Практический гайд для сборки сайтов через AI (Claude Code, Cursor).
> Каждый стиль и секция — с готовым промптом. Копируй, вставляй, получай компонент.

---

## Как пользоваться этим документом

1. **Выбери стиль** из каталога (Часть 1)
2. **Выбери секции** которые нужны (Часть 2)
3. **Проверь совместимость** по правилам смешивания (Часть 3)
4. **Скопируй промпт** и вставь в Claude Code / Cursor
5. **Допиши контекст** — свои цвета, шрифты, контент

### Формат промптов

Каждый промпт рассчитан на генерацию **React + Tailwind CSS** компонента.
Перед использованием подставь свои значения в `[КВАДРАТНЫЕ СКОБКИ]`.

### Базовый системный промпт (вставь в начало сессии)

```
Ты — senior frontend-разработчик и UX/UI дизайнер.
Стек: React 19, Next.js 16 (App Router), TypeScript, Tailwind CSS 4.
Правила:
- Компоненты: функциональные, с TypeScript типами
- Стили: только Tailwind utility classes, никакого inline CSS
- Адаптивность: mobile-first (sm → md → lg → xl)
- Анимации: Framer Motion или CSS transitions
- Доступность: ARIA labels, semantic HTML, keyboard navigation
- Spacing: кратно 4px (p-1=4px, p-2=8px, p-4=16px, p-6=24px, p-8=32px)
- Не используй Lorem ipsum — пиши реалистичный placeholder-текст
```

---

# ЧАСТЬ 1: КАТАЛОГ СТИЛЕЙ (20 направлений)

---

## 1. Minimalism

**Суть:** Максимум воздуха, минимум элементов. Каждый элемент на своём месте. Ничего лишнего.

**CSS-характеристики:**
- Много `padding` и `margin` (py-20, py-32)
- `max-w-3xl` или `max-w-4xl` для контента
- Цвета: 2-3 максимум (чёрный, белый, один accent)
- Шрифт: тонкий/light для заголовков
- Нет теней, нет градиентов, нет border
- `font-light`, `tracking-tight`, `leading-relaxed`

**Когда использовать:**
- Премиальные продукты, luxury-бренды
- Когда контент сам по себе сильный (фото, типографика)
- Для "дышащих" hero-секций

**Сочетается с:** Swiss Grid, Editorial, Organic, Card-based

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле минимализм.
Характеристики:
- Максимум пустого пространства (py-24 lg:py-32)
- Контент в узком контейнере (max-w-4xl mx-auto)
- Цвета: [BACKGROUND], [FOREGROUND], [ACCENT]
- Шрифт заголовка: font-light tracking-tight text-5xl lg:text-7xl
- Никаких теней, границ, градиентов
- Текст и один CTA — больше ничего
- Анимация: только fade-in при скролле (opacity 0→1, translateY 20→0)
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 2. Swiss / International Style

**Суть:** Строгая сетка, чёткая иерархия, асимметричные но математически точные композиции. Родом из швейцарской школы дизайна 1950-х, но по-прежнему актуально.

**CSS-характеристики:**
- `grid-cols-12` с `gap-4` или `gap-6`
- Жёсткая типографическая шкала (контрастные размеры: 14px body, 72px headline)
- Sans-serif шрифт (Inter, Helvetica, Neue Haas)
- Акцентный цвет — один, яркий, используется дозированно
- Линии-разделители (`border-b`, `border-t`)
- Текст выровнен по сетке, никогда не "плавает"

**Когда использовать:**
- Информационно-насыщенные страницы
- Когда нужна структура и порядок
- Портфолио, agency, корпоративные сайты

**Сочетается с:** Minimalism, Editorial, Monochrome Typography, Split-screen

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле Swiss/International Design.
Характеристики:
- 12-колоночная сетка (grid-cols-12)
- Жёсткая типографическая иерархия:
  - Заголовок: text-6xl lg:text-8xl font-bold tracking-tighter uppercase
  - Подзаголовок: text-sm font-medium uppercase tracking-widest text-muted
  - Body: text-base leading-relaxed max-w-prose
- Асимметричная композиция (контент не по центру, а col-span-7 + col-span-5)
- Один акцентный цвет: [ACCENT] — только для ключевых элементов
- Горизонтальные линии-разделители (border-t border-[BORDER_COLOR])
- Никаких скруглений (rounded-none), никаких теней
- Шрифт: [FONT_FAMILY]
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 3. Brutalism

**Суть:** Анти-дизайн. Сырой, грубый, неполированный. Видна "механика" — границы, моноширинные шрифты, системные цвета. Привлекает внимание именно своей необычностью.

**CSS-характеристики:**
- `border-2` или `border-4` на всём (чёрный)
- `font-mono` для заголовков
- Системные цвета или кричащие контрасты (жёлтый + чёрный)
- `shadow-[4px_4px_0_black]` — жёсткие тени без blur
- Нет `rounded-*` — всё квадратное
- Текст может быть UPPERCASE, огромного размера
- Курсор может быть кастомным

**Когда использовать:**
- Креативные агентства, арт-проекты
- Когда хочется выделиться из массы "красивых" сайтов
- Стартапы, tech-продукты с характером

**Сочетается с:** Neo-brutalism, Monochrome Typography, Retro/Y2K

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле brutalism.
Характеристики:
- Жёсткие чёрные рамки на всех элементах (border-2 border-black)
- Тени без blur: shadow-[4px_4px_0px_#000] (сдвиг, не размытие)
- Моноширинный шрифт для заголовков (font-mono)
- Квадратные углы везде (rounded-none)
- Кричащие цвета фона: [ЦВЕТ] (жёлтый #FFE600, или ярко-зелёный, или белый)
- Заголовок: text-4xl lg:text-7xl font-black uppercase
- Hover-эффект на кнопках: translate-x-[2px] translate-y-[2px] shadow-none (тень исчезает, кнопка "вдавливается")
- Никаких градиентов, никакого blur
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 4. Corporate Clean

**Суть:** Профессиональный, вызывающий доверие. Stripe, Linear, Notion до редизайна. Чистый, но не пустой — есть структура, цвет, иконки.

**CSS-характеристики:**
- `rounded-lg` или `rounded-xl` (не full, не none)
- Мягкие тени: `shadow-sm`, `shadow-md`
- Нейтральная палитра + один brand-цвет
- `text-gray-600` для body, `text-gray-900` для заголовков
- Иконки: Lucide или Heroicons, одноцветные
- `bg-white` карточки на `bg-gray-50` фоне

**Когда использовать:**
- SaaS, финтех, B2B продукты
- Когда нужно доверие и профессионализм
- Для dashboard-like лендингов

**Сочетается с:** Minimalism, Card-based, Glassmorphism, Bento Grid

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле Corporate Clean.
Характеристики:
- Чистые карточки: bg-white rounded-xl shadow-sm border border-gray-100
- Типографика: text-gray-900 font-semibold для заголовков, text-gray-600 для описаний
- Иконки: простые линейные (Lucide React), цвет [ACCENT]
- Кнопки: bg-[ACCENT] text-white rounded-lg px-6 py-3 font-medium
- Hover: hover:shadow-md transition-shadow
- Контейнер: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
- Фон секции: bg-gray-50 или bg-white чередование
- Spacing: py-16 lg:py-24 между секциями
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 5. Bento Grid

**Суть:** Сетка из карточек разного размера — как у Apple на страницах продуктов. Каждая ячейка — самостоятельный блок с фичей, картинкой или метрикой. Визуально интересно, но структурировано.

**CSS-характеристики:**
- `grid-cols-2 md:grid-cols-4` с `gap-4`
- Ячейки: `col-span-1`, `col-span-2`, `row-span-2`
- Одна "hero-ячейка" — крупнее остальных (col-span-2 row-span-2)
- `rounded-2xl` или `rounded-3xl` на карточках
- Фон карточек: мягкие оттенки или градиенты
- `overflow-hidden` для карточек с изображениями
- `p-6` или `p-8` внутри карточек

**Когда использовать:**
- Feature-секции (показать 4-8 возможностей продукта)
- Портфолио, галереи
- Dashboard-like представление данных

**Сочетается с:** Corporate Clean, Minimalism, Dark Premium, Glassmorphism

**Промпт:**
```
Создай Bento Grid секцию для отображения [ЧТО ПОКАЗЫВАЕМ].
Характеристики:
- Сетка: grid grid-cols-2 md:grid-cols-4 gap-4
- Hero-ячейка: col-span-2 row-span-2 (самый важный элемент)
- Карточки: rounded-2xl p-6 lg:p-8
- Цвета карточек — чередование мягких оттенков:
  - bg-[COLOR_1] (основной)
  - bg-[COLOR_2] (вторичный)
  - bg-[COLOR_3] (акцент, только для hero-ячейки)
- Каждая карточка содержит: иконку/эмодзи + заголовок (text-lg font-semibold) + описание (text-sm text-muted)
- Hero-ячейка: крупный заголовок (text-2xl) + иллюстрация или большая иконка
- Hover: hover:scale-[1.02] transition-transform
- На мобиле: grid-cols-1, все ячейки одного размера (col-span-1 row-span-1)
Контент: [СПИСОК ФИЧЕЙ/ЭЛЕМЕНТОВ С ОПИСАНИЯМИ]
```

---

## 6. Neo-brutalism

**Суть:** Эволюция брутализма. Те же жёсткие тени и рамки, но с яркими цветами, скруглениями и дружелюбной энергией. Gumroad, Figma Community, многие indie-продукты.

**CSS-характеристики:**
- `border-2 border-black` + `shadow-[4px_4px_0_black]`
- НО: `rounded-xl` или `rounded-2xl` (скруглено!)
- Яркие пастельные фоны: лавандовый, мятный, персиковый, лимонный
- `font-bold` или `font-black` для заголовков
- Кнопки "вдавливаются" при клике
- Иллюстрации или эмодзи вместо иконок

**Когда использовать:**
- Indie-продукты, творческие инструменты
- Продукты для молодой аудитории
- Когда хочется характер, но не агрессию

**Сочетается с:** Brutalism, Illustrated, Card-based, Retro/Y2K

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле neo-brutalism.
Характеристики:
- Все карточки и кнопки: border-2 border-black rounded-xl shadow-[5px_5px_0px_#000]
- Яркие фоны карточек (каждая — свой цвет):
  - #FFE8E8 (розовый)
  - #E8FFE8 (мятный)
  - #E8E8FF (лавандовый)
  - #FFF8E8 (персиковый)
- Заголовки: font-black text-black
- Кнопки: при hover — translate-x-[2px] translate-y-[2px] shadow-[2px_2px_0px_#000]
  При active — translate-x-[4px] translate-y-[4px] shadow-none
- Эмодзи вместо иконок (🚀 📦 ✨ 🎯)
- Фон секции: bg-[LIGHT_COLOR] или белый
- Энергичная, дружелюбная подача текста
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 7. Glassmorphism

**Суть:** Полупрозрачные элементы с размытием фона. Как матовое стекло. iOS, macOS, Windows 11. Элегантно, но нужен правильный фон.

**CSS-характеристики:**
- `backdrop-blur-xl` + `bg-white/10` (или bg-white/20)
- `border border-white/20` — тонкая светлая граница
- `rounded-2xl` или `rounded-3xl`
- Фон: обязательно яркий — градиент, изображение, или mesh
- `shadow-xl` с прозрачностью
- Текст: белый или очень тёмный (зависит от фона)

**Когда использовать:**
- Поверх ярких фонов или градиентов
- Карточки поверх hero-секций
- Модальные окна, login-формы
- Когда нужен эффект "премиум"

**Сочетается с:** Dark Premium, Aurora/Gradient, Corporate Clean, Minimalism

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле glassmorphism.
Характеристики:
- Фон секции: gradient от [COLOR_1] к [COLOR_2] (яркий, насыщенный)
- Карточки/контейнеры поверх:
  - bg-white/10 backdrop-blur-xl
  - border border-white/20
  - rounded-2xl
  - shadow-[0_8px_32px_rgba(0,0,0,0.12)]
- Текст: text-white (на тёмном фоне) или text-gray-900 (на светлом)
- Внутренние элементы: тоже полупрозрачные (bg-white/5 rounded-lg p-4)
- Hover: bg-white/20 transition-colors
- ВАЖНО: backdrop-blur работает только с полупрозрачным bg
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 8. Claymorphism

**Суть:** Мягкие, "пухлые" 3D-элементы. Как из пластилина или мягкой глины. Два слоя теней (внешняя + внутренняя) создают объём.

**CSS-характеристики:**
- Двойная тень: `shadow-[8px_8px_16px_#d1d9e6,-8px_-8px_16px_#ffffff]`
- Внутренняя тень: `inset shadow` для эффекта "выдавленности"
- `rounded-2xl` или `rounded-3xl`
- Пастельные, приглушённые цвета
- `bg-[#f0f0f3]` — нейтральный серо-белый фон
- Нет резких контрастов

**Когда использовать:**
- Детские/образовательные продукты
- Dashboard с мягким UI
- Приложения для здоровья, медитации

**Сочетается с:** Organic, Glassmorphism, Card-based

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле claymorphism.
Характеристики:
- Фон: bg-[#f0f0f3]
- Карточки:
  - bg-[#f0f0f3]
  - rounded-3xl
  - shadow-[8px_8px_16px_#d1d9e6,-8px_-8px_16px_#ffffff]
  - p-8
- Кнопки: такие же тени + при нажатии — inset shadow (вдавливание):
  active:shadow-[inset_4px_4px_8px_#d1d9e6,inset_-4px_-4px_8px_#ffffff]
- Цветные акценты: мягкие пастельные ([PASTEL_ACCENT])
- Иконки: мягкие, скруглённые (Lucide), в пастельных кругах
- Заголовки: font-semibold text-gray-800
- Текст: text-gray-500
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 9. Aurora / Gradient Mesh

**Суть:** Плавные многоцветные градиенты, как северное сияние. Mesh-градиенты из 3-4 цветов, создающие органичные переливы. Stripe, Linear, Raycast.

**CSS-характеристики:**
- Сложные градиенты: `bg-gradient-to-br from-[COLOR_1] via-[COLOR_2] to-[COLOR_3]`
- Или CSS `radial-gradient` / `conic-gradient` для mesh-эффекта
- Animated: gradient-position меняется по анимации
- `blur-3xl` на фоновых blob-элементах
- Текст: белый на тёмных градиентах, тёмный на светлых
- Часто комбинируется с glassmorphism для карточек

**Когда использовать:**
- Hero-секции (wow-эффект)
- Фоны для CTA-секций
- Премиальные продукты, AI-продукты

**Сочетается с:** Glassmorphism, Dark Premium, Minimalism, Immersive

**Промпт:**
```
Создай [ТИП СЕКЦИИ] с aurora/gradient mesh фоном.
Характеристики:
- Фон: тёмный (bg-gray-950 или bg-black)
- Поверх фона — 3-4 градиентных blob-а:
  - Каждый: absolute, w-[500px] h-[500px], rounded-full, blur-3xl, opacity-30
  - Цвета: [COLOR_1], [COLOR_2], [COLOR_3]
  - Позиции: разбросаны по секции (top-0 left-1/4, bottom-0 right-1/3, etc.)
- Контейнер: relative z-10 (контент поверх blob-ов)
- Текст: text-white
- Для анимации: animate-pulse на blob-ах (slow, 4-8s) или кастомная CSS animation
  @keyframes aurora { 0% { transform: translate(0,0) } 50% { transform: translate(30px,-30px) } 100% { transform: translate(0,0) } }
- Карточки поверх: glassmorphism (bg-white/5 backdrop-blur-xl border border-white/10)
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 10. Dark Mode Premium

**Суть:** Тёмный фон, тонкие линии, неоновые или приглушённые акценты. Ассоциация с luxury, tech, профессионализмом. Linear, Vercel, Raycast.

**CSS-характеристики:**
- `bg-gray-950` или `bg-[#0A0A0A]` — не чисто чёрный
- Текст: `text-gray-100` заголовки, `text-gray-400` body
- Акценты: неон (#00FF88, #7C3AED, #3B82F6) или приглушённые
- Границы: `border-gray-800` или `border-white/10`
- Тонкие gradient-линии как разделители
- `hover:bg-white/5` — едва заметный hover

**Когда использовать:**
- Dev tools, AI-продукты, tech-стартапы
- Премиум-тарифы, luxury
- Ночная тема, космическая тематика

**Сочетается с:** Aurora, Glassmorphism, Minimalism, Motion-first

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле Dark Premium.
Характеристики:
- Фон: bg-[#0A0A0F]
- Текст: text-gray-100 для заголовков, text-gray-400 для body
- Акцентный цвет: [ACCENT] (один яркий, используется точечно)
- Карточки: bg-white/[3-5%] border border-white/10 rounded-xl
- Hover на карточках: hover:border-[ACCENT]/30 hover:bg-white/[5-8%] transition-all
- Разделители: gradient-линия (h-px bg-gradient-to-r from-transparent via-gray-700 to-transparent)
- Кнопка CTA: bg-[ACCENT] text-white rounded-lg, или ghost: border border-[ACCENT] text-[ACCENT]
- Spacing: щедрый (py-24 lg:py-32)
- Мелкие детали: точки-grid на фоне (bg-[radial-gradient(#333_1px,transparent_1px)] bg-[size:24px_24px])
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 11. Editorial / Magazine

**Суть:** Как журнальная вёрстка: крупные заголовки, асимметрия, контрастные размеры шрифтов, колоночная раскладка. Vogue, Bloomberg, NYT.

**CSS-характеристики:**
- Огромные заголовки: `text-7xl` или `text-9xl`
- Контраст размеров: заголовок 96px, body 16px
- Serif + Sans-serif combo
- Asymmetric grid: `grid-cols-5` → `col-span-3` + `col-span-2`
- Pull-quotes (крупные цитаты)
- Декоративные элементы: линии, номера, uppercase labels

**Когда использовать:**
- Контентные сайты, блоги, медиа
- About-страницы, истории
- Когда текст — главный элемент

**Сочетается с:** Swiss Grid, Minimalism, Split-screen, Monochrome Typography

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле Editorial/Magazine.
Характеристики:
- Заголовок: text-6xl md:text-8xl lg:text-9xl font-serif font-bold tracking-tight leading-[0.9]
- Контрастная типографика: заголовок serif, body sans-serif
- Асимметричная сетка: grid grid-cols-1 md:grid-cols-5 gap-8
  - Текст: col-span-3
  - Изображение или pull-quote: col-span-2
- Декоративные элементы:
  - Uppercase labels: text-xs uppercase tracking-widest text-muted font-medium
  - Номер секции: text-8xl font-bold text-muted/20 (как фоновый элемент)
  - Тонкая линия: w-12 h-px bg-black mb-6
- Line-height для body: leading-relaxed (1.75)
- Цитата: text-2xl font-serif italic border-l-4 border-[ACCENT] pl-6
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 12. Retro / Y2K

**Суть:** Ностальгия по интернету 2000-х. Яркие кислотные цвета, пиксельные элементы, chrome-эффекты, звёзды, градиенты из фуксии в циан.

**CSS-характеристики:**
- Кислотные цвета: фуксия, циан, лайм, жёлтый
- `bg-gradient-to-r from-fuchsia-500 via-cyan-400 to-yellow-300`
- Пиксельные шрифты (Press Start 2P) или намеренно "дешёвые"
- Звёзды, sparkles как декор ✦ ✧ ★
- Chrome-текст: gradient на тексте (bg-clip-text text-transparent)
- `border-4` яркого цвета
- Sticker-эффекты: элементы "наклеены" под углом (`rotate-[-3deg]`)

**Когда использовать:**
- Развлекательные проекты, event-лендинги
- Молодёжная аудитория, Gen Z
- Музыка, мода, поп-культура

**Сочетается с:** Neo-brutalism, Brutalism, Illustrated

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле Retro/Y2K.
Характеристики:
- Фон: bg-gradient-to-br from-fuchsia-600 via-purple-600 to-cyan-500
  или чёрный с неоновыми акцентами
- Текст заголовков: bg-gradient-to-r from-yellow-300 via-pink-400 to-cyan-400 bg-clip-text text-transparent
  font-black text-5xl uppercase
- Декоративные звёзды: ✦ ✧ ⋆ разбросаны вокруг контента (absolute, разные размеры)
- Элементы под углом: rotate-[-2deg] или rotate-[3deg] на карточках (стикер-эффект)
- Рамки: border-4 border-[ЯРКИЙ_ЦВЕТ] rounded-lg
- Тени: shadow-[0_0_20px_rgba(255,0,255,0.5)] (glow-эффект)
- Кнопки: яркие, с glow при hover
- Можно добавить CSS-grid с pixel-art фоном
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 13. Organic / Soft

**Суть:** Скруглённые формы, blob-shapes, пастельные цвета, мягкие тени. Природная, "живая" эстетика. Headspace, Calm, многие health-tech приложения.

**CSS-характеристики:**
- `rounded-3xl` или `rounded-[2rem]` на всём
- Пастельные цвета: мятный, лавандовый, персиковый, кремовый
- Blob-формы на фоне: `border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%`
- Мягкие тени: `shadow-xl shadow-[COLOR]/10`
- `leading-relaxed`, много воздуха
- Градиенты — нежные, между близкими оттенками

**Когда использовать:**
- Wellness, здоровье, образование
- Детские продукты
- Когда нужно ощущение безопасности и уюта

**Сочетается с:** Minimalism, Illustrated, Claymorphism, Card-based

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле Organic/Soft.
Характеристики:
- Все углы: максимально скруглены (rounded-3xl или rounded-[2rem])
- Цвета: пастельные — [PASTEL_BG], [PASTEL_ACCENT], [PASTEL_SECONDARY]
- Blob-формы на фоне (декоративные):
  - absolute, w-64 h-64, bg-[PASTEL_COLOR]/30, blur-2xl
  - border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%
  - animate-[blob_7s_ease-in-out_infinite] для медленного движения
- Тени: мягкие, цветные — shadow-xl shadow-[PASTEL_COLOR]/20
- Типографика: font-medium (не bold), leading-relaxed
- Кнопки: rounded-full, мягкий цвет, без жёстких контрастов
- Spacing: щедрый (py-20 lg:py-28)
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 14. Motion-first

**Суть:** Анимация — основа UX. Scroll-triggered появления, parallax, morphing, staggered анимации. Awwwards-уровень. Stripe, Apple, многие agency-сайты.

**CSS-характеристики:**
- Framer Motion: `useScroll`, `useTransform`, `useInView`
- Staggered animations: delay 0.1s между элементами
- `whileInView={{ opacity: 1, y: 0 }}`
- Parallax: разная скорость скролла для слоёв
- `transition: { duration: 0.6, ease: [0.22, 1, 0.36, 1] }` (custom easing)
- `will-change-transform` для производительности

**Когда использовать:**
- Portfolio, agency-сайты
- Продуктовые лендинги (показать фичи через анимацию)
- Когда нужен wow-эффект

**Сочетается с:** любой стиль + анимация как overlay-слой

**Промпт:**
```
Создай [ТИП СЕКЦИИ] с продвинутыми анимациями (motion-first).
Используй Framer Motion.
Характеристики:
- Все элементы появляются при скролле (useInView):
  initial={{ opacity: 0, y: 40 }}
  whileInView={{ opacity: 1, y: 0 }}
  viewport={{ once: true, margin: "-100px" }}
  transition={{ duration: 0.7, ease: [0.22, 1, 0.36, 1] }}
- Staggered animation для списков:
  Каждый элемент: delay = index * 0.1
  (через staggerChildren: 0.1 в parent variants)
- Заголовок: split по словам, каждое слово появляется отдельно (stagger 0.05s)
- Hover на карточках: whileHover={{ y: -8, transition: { duration: 0.3 } }}
- Parallax для фоновых элементов: useScroll + useTransform(scrollY, [start, end], [0, -50])
- ОБЯЗАТЕЛЬНО: prefers-reduced-motion — отключить все анимации
  const prefersReducedMotion = useReducedMotion()
Контент: [ОПИСАНИЕ КОНТЕНТА]
Визуальный стиль: [ОДИН ИЗ СТИЛЕЙ ВЫШЕ]
```

---

## 15. Data-driven Dashboard

**Суть:** Плотная компоновка, метрики, графики, таблицы. Тёмная тема, моноширинные числа, status-бейджи. Vercel Dashboard, Grafana, Datadog.

**CSS-характеристики:**
- `bg-gray-950` фон, `bg-gray-900` карточки
- `font-mono` для чисел и метрик
- Мелкий текст: `text-xs`, `text-sm`
- Status-цвета: green-500, yellow-500, red-500
- Dense grid: `gap-3` или `gap-4`
- Sparkline-графики, progress bars
- Borders: `border-gray-800`

**Когда использовать:**
- Dashboard-секции на лендингах ("вот как выглядит продукт")
- Analytics, monitoring продукты
- Для демонстрации метрик

**Сочетается с:** Dark Premium, Corporate Clean, Bento Grid

**Промпт:**
```
Создай dashboard-секцию для отображения [ЧТО ПОКАЗЫВАЕМ].
Характеристики:
- Фон: bg-gray-950, карточки: bg-gray-900 border border-gray-800 rounded-lg
- Сетка метрик сверху: grid grid-cols-2 lg:grid-cols-4 gap-4
  Каждая метрика: label (text-xs text-gray-500 uppercase) + значение (text-2xl font-mono font-bold text-white) + тренд (text-xs text-green-500 "↑ 12%")
- Основная область: графики или таблицы
- Status badges: inline-flex items-center rounded-full px-2 py-0.5 text-xs
  - Зелёный: bg-green-500/10 text-green-500
  - Жёлтый: bg-yellow-500/10 text-yellow-500
  - Красный: bg-red-500/10 text-red-500
- Таблица: divide-y divide-gray-800, hover:bg-gray-800/50 на строках
- Font: font-mono для всех числовых значений
Контент: [ОПИСАНИЕ МЕТРИК/ДАННЫХ]
```

---

## 16. Illustrated / Hand-drawn

**Суть:** Кастомные иллюстрации или sketch-стиль определяют визуал. Notion, Dropbox, Mailchimp. Illustration как часть UI, не как декорация.

**CSS-характеристики:**
- Иллюстрации: SVG, встроенные или компоненты
- Мягкие тени: `shadow-lg`
- Rounded: `rounded-2xl`
- Много белого пространства вокруг иллюстраций
- Цвета: из палитры иллюстраций
- Шрифты: friendly (rounded sans-serif)

**Когда использовать:**
- Onboarding, пустые состояния, error pages
- Продукты с характером и индивидуальностью
- Объяснения сложных концепций

**Сочетается с:** Organic, Minimalism, Card-based, Neo-brutalism

**Промпт:**
```
Создай [ТИП СЕКЦИИ] с illustrated/hand-drawn стилем.
Характеристики:
- Каждый элемент/фича сопровождается SVG-иллюстрацией или крупным эмодзи (placeholder)
- Иллюстрации: размер 200x200 или 300x300, в мягких кругах (bg-[PASTEL]/20 rounded-full p-8)
- Layout: чередование (illustration left + text right, потом наоборот)
  Или: grid grid-cols-1 md:grid-cols-3 с иллюстрацией сверху в каждой колонке
- Типографика: friendly — font-medium (не bold), text-gray-700, leading-relaxed
- Фон: чистый белый или кремовый
- Вместо реальных иллюстраций — placeholder:
  <div className="w-48 h-48 bg-[PASTEL]/10 rounded-full flex items-center justify-center text-6xl">[EMOJI]</div>
- Hover: мягкий scale (hover:scale-105 transition-transform)
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 17. Monochrome Typography

**Суть:** Один цвет (или чёрно-белый), огромные буквы — типографика IS дизайн. Контраст размеров, weights, leading. Как музыкальный постер или art gallery.

**CSS-характеристики:**
- Заголовки: `text-8xl` до `text-[12rem]`
- `font-black` + `font-light` контраст
- `tracking-tighter` на крупных заголовках
- `leading-none` или `leading-[0.85]`
- Один цвет + оттенки серого
- `mix-blend-mode: difference` для инверсии
- `text-stroke` для outline-текста

**Когда использовать:**
- Hero-секции с мощным заявлением
- Portfolio, agency
- Когда нет изображений, но нужен сильный визуал

**Сочетается с:** Swiss Grid, Minimalism, Editorial, Brutalism

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле Monochrome Typography.
Характеристики:
- Только чёрный + белый (или один accent-цвет)
- Заголовок: text-[8rem] lg:text-[12rem] font-black leading-none tracking-tighter
  или: text-[8rem] font-light (тонкий гигантский текст)
- Контраст weight-ов: одно слово font-black, следующее font-light
- Outline-текст: text-transparent [-webkit-text-stroke:2px_black]
  (через стили: style={{ WebkitTextStroke: '2px black' }})
- Минимум других элементов: только текст + одна CTA-кнопка
- Фон: чистый белый или чёрный
- Можно: текст выходит за экран (overflow-hidden, text крупнее viewport)
- Body текст: контрастно мелкий (text-sm max-w-md)
Контент: [ТЕКСТ ЗАГОЛОВКА — максимально короткий и мощный]
```

---

## 18. Card-based UI

**Суть:** Всё — через карточки. Каждый элемент информации в своём контейнере. Pinterest, Trello, App Store. Модульно и сканируемо.

**CSS-характеристики:**
- `rounded-xl` или `rounded-2xl`
- `shadow-sm` + `hover:shadow-lg`
- `border border-gray-200`
- `overflow-hidden` для карточек с изображениями
- Masonry или равномерная сетка
- `group` + `group-hover:` для внутренних анимаций

**Когда использовать:**
- Каталоги, списки товаров
- Блоги, портфолио
- Feature-секции, отзывы

**Сочетается с:** Corporate Clean, Minimalism, Bento Grid, Organic

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле Card-based UI.
Характеристики:
- Сетка: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
- Каждая карточка:
  - group rounded-xl border border-[BORDER] bg-[CARD_BG] overflow-hidden
  - Hover: hover:shadow-xl hover:border-[ACCENT]/30 transition-all duration-300
  - Hover подъём: hover:-translate-y-1
- Структура карточки:
  - Изображение сверху: aspect-video w-full object-cover, group-hover:scale-105 transition-transform
  - Контент: p-6
  - Tag/category: text-xs uppercase tracking-wide text-[ACCENT] font-medium
  - Заголовок: text-lg font-semibold mt-2
  - Описание: text-sm text-muted mt-2 line-clamp-3
  - Footer: flex justify-between items-center mt-4 pt-4 border-t
- На мобиле: 1 колонка, на планшете 2, на десктопе 3
Контент: [СПИСОК КАРТОЧЕК С ДАННЫМИ]
```

---

## 19. Split-screen

**Суть:** Экран делится на две контрастные половины. Текст слева, визуал справа (или наоборот). Создаёт драматический контраст. Часто используется в hero и feature-секциях.

**CSS-характеристики:**
- `grid-cols-2` на десктопе, `grid-cols-1` на мобиле
- Две половины могут быть контрастных цветов
- Одна половина — текст, другая — изображение/видео/иллюстрация
- Полная высота: `min-h-screen` или `min-h-[80vh]`
- Каждая половина: `flex items-center justify-center p-12`

**Когда использовать:**
- Hero-секции (мощный первый экран)
- Before/After сравнения
- Feature highlight (текст + demo)

**Сочетается с:** Minimalism, Editorial, Dark Premium, Monochrome Typography

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле Split-screen.
Характеристики:
- Layout: grid grid-cols-1 lg:grid-cols-2 min-h-[80vh]
- Левая половина (контент):
  - bg-[COLOR_1] flex items-center
  - Внутренний контейнер: max-w-xl p-8 lg:p-16
  - Заголовок: text-4xl lg:text-5xl font-bold tracking-tight
  - Текст: text-lg text-muted leading-relaxed mt-6
  - CTA: mt-8
- Правая половина (визуал):
  - bg-[COLOR_2] flex items-center justify-center
  - Изображение: object-cover w-full h-full (или скриншот, или иллюстрация)
  - Или: decorative pattern / gradient / animated element
- На мобиле: стакается (визуал сверху, контент снизу)
- Контраст: одна половина светлая, другая тёмная (или accent)
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

## 20. Immersive / Full-screen

**Суть:** Полноэкранные фото, видео или фоны с контентом поверх. Максимальный визуальный удар. Apple, Tesla, luxury-бренды.

**CSS-характеристики:**
- `min-h-screen` + `relative`
- Фон: `object-cover w-full h-full absolute inset-0`
- Overlay: `bg-black/40` или gradient overlay
- Контент: `relative z-10 text-white`
- `text-center` или `text-left` с `max-w-2xl`
- Scroll-snap для нескольких full-screen секций

**Когда использовать:**
- Когда есть мощные фото/видео
- Luxury, travel, automotive бренды
- Один ключевой message на весь экран

**Сочетается с:** Minimalism, Motion-first, Dark Premium, Monochrome Typography

**Промпт:**
```
Создай [ТИП СЕКЦИИ] в стиле Immersive/Full-screen.
Характеристики:
- Контейнер: relative min-h-screen flex items-center justify-center overflow-hidden
- Фон: absolute inset-0
  - Вариант A (изображение): <img src="[URL]" className="object-cover w-full h-full" />
  - Вариант B (видео): <video autoPlay muted loop playsInline className="object-cover w-full h-full" />
  - Вариант C (gradient): bg-gradient-to-b from-[COLOR_1] to-[COLOR_2]
- Overlay поверх фона: absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent
- Контент: relative z-10 text-center text-white max-w-3xl mx-auto px-4
  - Заголовок: text-5xl lg:text-7xl font-bold tracking-tight
  - Подзаголовок: text-xl text-white/80 mt-6 max-w-xl mx-auto
  - CTA: mt-10
- Scroll indicator внизу: animate-bounce, стрелка вниз
Контент: [ОПИСАНИЕ КОНТЕНТА]
```

---

# ЧАСТЬ 2: ПРОМПТЫ ДЛЯ КАЖДОГО ТИПА СЕКЦИИ

Ниже — промпты для 30 типов секций. Каждый промпт — стиль-агностик (подставляй любой стиль из Части 1).

---

## НАВИГАЦИЯ

### Секция: Navbar

```
Создай компонент Navbar (header навигация).
Структура:
- Контейнер: sticky top-0 z-50 bg-[BACKGROUND]/80 backdrop-blur-md border-b border-[BORDER]
- Внутри: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
- Flex layout: flex items-center justify-between h-16 lg:h-20
- Слева: лого (текст или SVG) — text-xl font-bold
- Центр (desktop): nav links — flex gap-8 text-sm font-medium text-[MUTED]
  hover:text-[FOREGROUND] transition-colors
- Справа: CTA кнопка — [СТИЛЬ КНОПКИ]
- Мобильное меню:
  - Бургер-кнопка (md:hidden): 3 линии, анимация в X при открытии
  - Меню: fixed inset-0 bg-[BACKGROUND] z-50, flex flex-col items-center justify-center gap-8
  - Пункты меню: text-2xl font-medium
- Навигация: [СПИСОК ПУНКТОВ МЕНЮ]
```

### Секция: Mega Menu

```
Создай Mega Menu — раскрывающееся навигационное меню.
Структура:
- Trigger: hover или click на nav-item
- Dropdown: absolute top-full left-0 w-full bg-[BACKGROUND] border-b border-[BORDER] shadow-xl
  py-8
- Внутри: max-w-7xl mx-auto grid grid-cols-4 gap-8 px-8
- Каждая колонка:
  - Заголовок группы: text-xs uppercase tracking-widest text-[MUTED] font-medium mb-4
  - Ссылки: flex flex-col gap-2
    Каждая: flex items-start gap-3 p-2 rounded-lg hover:bg-[SECONDARY]
    - Иконка (20x20)
    - Div: name (text-sm font-medium) + description (text-xs text-[MUTED])
- Анимация: opacity 0→1, translateY -8→0, duration 200ms
- Overlay: fixed inset-0 bg-black/20 за меню (click = закрыть)
Разделы меню: [СПИСОК КАТЕГОРИЙ И ПУНКТОВ]
```

---

## HERO-СЕКЦИИ

### Секция: Hero — Centered

```
Создай Hero-секцию с центрированным контентом.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: relative min-h-[90vh] flex items-center justify-center py-20
- Контент: text-center max-w-4xl mx-auto px-4
- Над заголовком: badge/label — inline-flex items-center rounded-full px-4 py-1.5
  bg-[ACCENT]/10 text-[ACCENT] text-sm font-medium
  Текст: [LABEL — например "Новинка 2026" или "10 000+ студентов"]
- Заголовок: text-5xl md:text-6xl lg:text-7xl font-bold tracking-tight leading-[1.1]
  [ТЕКСТ ЗАГОЛОВКА]
- Подзаголовок: text-xl text-[MUTED] mt-6 max-w-2xl mx-auto leading-relaxed
  [ТЕКСТ ПОДЗАГОЛОВКА]
- CTA группа: flex flex-col sm:flex-row gap-4 justify-center mt-10
  - Primary: [СТИЛЬ КНОПКИ] px-8 py-4 text-lg
  - Secondary: ghost/outline вариант
- Под CTA: social proof — flex items-center justify-center gap-2 mt-8
  Аватарки + "Присоединились 10,000+ студентов"
```

### Секция: Hero — Split

```
Создай Hero-секцию с разделением на 2 части.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: max-w-7xl mx-auto px-4 py-20 lg:py-28
- Grid: grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center
- Левая часть (текст):
  - Badge сверху (опционально)
  - Заголовок: text-4xl lg:text-5xl xl:text-6xl font-bold tracking-tight
  - Описание: text-lg text-[MUTED] mt-6 leading-relaxed
  - CTA: flex flex-wrap gap-4 mt-8
  - Метрики (опционально): flex gap-8 mt-10
    Каждая: div — число (text-3xl font-bold) + label (text-sm text-[MUTED])
- Правая часть (визуал):
  - Изображение / скриншот / иллюстрация
  - rounded-2xl shadow-2xl overflow-hidden
  - Или: mockup телефона/ноутбука с интерфейсом внутри
- На мобиле: визуал первым (order-first lg:order-last), текст вторым
Контент: [ОПИСАНИЕ]
```

### Секция: Hero — Full-screen

```
Создай полноэкранную Hero-секцию.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: relative min-h-screen flex items-center overflow-hidden
- Фон: absolute inset-0
  [ВАРИАНТ ФОНА: изображение / видео / gradient / animated mesh]
- Overlay: absolute inset-0 bg-gradient-to-r from-black/70 via-black/50 to-transparent
- Контент: relative z-10 max-w-7xl mx-auto px-4
  - Ширина текста: max-w-2xl
  - Заголовок: text-5xl lg:text-7xl font-bold text-white tracking-tight
  - Подзаголовок: text-xl text-white/80 mt-6
  - CTA: flex gap-4 mt-10
    Primary: большая яркая кнопка
    Secondary: border border-white text-white (ghost)
- Scroll indicator: absolute bottom-8 left-1/2 -translate-x-1/2
  animate-bounce text-white/60
  Стрелка вниз или "Scroll" текст
Контент: [ОПИСАНИЕ]
```

---

## СОЦИАЛЬНОЕ ДОКАЗАТЕЛЬСТВО

### Секция: Logo Bar

```
Создай секцию с логотипами партнёров/клиентов.
Структура:
- Контейнер: py-12 lg:py-16 border-y border-[BORDER]
- Текст сверху (опционально): text-center text-sm text-[MUTED] mb-8
  "Нам доверяют 500+ компаний" или "Как видно в:"
- Логотипы: flex flex-wrap items-center justify-center gap-8 lg:gap-12
  Каждый логотип: h-8 lg:h-10 opacity-50 hover:opacity-100 transition-opacity grayscale hover:grayscale-0
- Вариант с анимацией (бегущая строка):
  - overflow-hidden
  - Внутри: flex gap-12 animate-[scroll_30s_linear_infinite]
  - Дублировать список логотипов для бесшовного цикла
  - @keyframes scroll { 0% { translateX(0) } 100% { translateX(-50%) } }
  - hover:pause (animation-play-state: paused)
Логотипы: [СПИСОК НАЗВАНИЙ — placeholder-ы как текст или SVG]
```

### Секция: Testimonials — Cards

```
Создай секцию отзывов в виде карточек.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28
- Заголовок секции: text-center mb-16
  - Label: text-sm uppercase tracking-widest text-[ACCENT] font-medium
  - H2: text-3xl lg:text-4xl font-bold mt-2
- Сетка: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
- Каждая карточка:
  - bg-[CARD] rounded-xl p-6 lg:p-8 border border-[BORDER]
  - Звёзды сверху: flex gap-1 text-yellow-500 (★★★★★)
  - Цитата: text-[FOREGROUND] mt-4 leading-relaxed "[ТЕКСТ ОТЗЫВА]"
  - Автор (внизу): flex items-center gap-3 mt-6 pt-6 border-t border-[BORDER]
    - Аватар: w-10 h-10 rounded-full bg-[ACCENT]/10
    - Div: имя (text-sm font-medium) + должность (text-xs text-[MUTED])
- Hover на карточке: hover:shadow-lg hover:-translate-y-1 transition-all
Отзывы: [МАССИВ: { quote, author, role, rating }]
```

### Секция: Testimonials — Wall (Masonry)

```
Создай стену отзывов в стиле masonry (разная высота карточек).
Структура:
- Контейнер: py-20 lg:py-28
- Заголовок секции по центру
- Masonry layout: columns-1 md:columns-2 lg:columns-3 gap-6 space-y-6
  (CSS columns создают masonry-эффект без JS)
- Каждый отзыв: break-inside-avoid
  - bg-[CARD] rounded-xl p-6 border border-[BORDER]
  - Цитата (разной длины — это создаёт masonry-эффект)
  - Автор: flex items-center gap-3 mt-4
- Вариант: gradient-fade снизу (mask-image: linear-gradient(to bottom, black 60%, transparent))
  + кнопка "Показать все отзывы"
Отзывы: [МАССИВ РАЗНОЙ ДЛИНЫ — короткие и длинные]
```

### Секция: Stats / Metrics Bar

```
Создай секцию с ключевыми метриками/цифрами.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-16 bg-[ACCENT] text-white (или bg-[SECONDARY])
- Внутри: max-w-7xl mx-auto px-4
- Сетка: grid grid-cols-2 lg:grid-cols-4 gap-8
- Каждая метрика: text-center
  - Число: text-4xl lg:text-5xl font-bold
    Анимация: CountUp от 0 до значения при появлении в viewport
    (useInView + useSpring/useMotionValue из Framer Motion)
  - Суффикс: "+" или "K" или "%" рядом с числом
  - Описание: text-sm opacity-80 mt-2
- Разделители между метриками (опционально): border-l border-white/20 (кроме первого)
Метрики: [МАССИВ: { value: "10K+", label: "Студентов" }]
```

---

## КОНТЕНТ И ПРЕИМУЩЕСТВА

### Секция: Features Grid

```
Создай секцию с features/преимуществами в сетке.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28
- Заголовок: text-center max-w-2xl mx-auto mb-16
  - Label + H2 + описание (text-lg text-[MUTED])
- Сетка: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8
  (или lg:grid-cols-4 для 4 features)
- Каждая feature:
  - Иконка сверху: w-12 h-12 rounded-xl bg-[ACCENT]/10 flex items-center justify-center
    text-[ACCENT] (Lucide icon внутри)
  - Заголовок: text-lg font-semibold mt-4
  - Описание: text-sm text-[MUTED] mt-2 leading-relaxed
- Hover: [ПО СТИЛЮ — подъём/тень/цвет]
- Вариант: с карточками (bg-[CARD] p-6 rounded-xl border)
Features: [МАССИВ: { icon, title, description }]
```

### Секция: Bento Features

```
Создай Bento Grid для features.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28
- Заголовок секции по центру
- Сетка: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4
- Распределение ячеек (пример для 6 features):
  1. col-span-2 row-span-2 (hero feature — главная)
  2. col-span-2 row-span-1
  3. col-span-1 row-span-1
  4. col-span-1 row-span-1
  5. col-span-2 row-span-1
  6. col-span-2 row-span-1
- Каждая ячейка: bg-[CARD] rounded-2xl p-6 lg:p-8 border border-[BORDER] overflow-hidden
- Hero-ячейка: крупный заголовок + описание + визуальный элемент (скриншот, иллюстрация)
- Обычные ячейки: иконка + заголовок + короткое описание
- На мобиле: все col-span-1 row-span-1 (одна колонка)
Features: [МАССИВ: { title, description, size: "hero" | "wide" | "normal" }]
```

### Секция: Feature Showcase (одна фича на экран)

```
Создай Feature Showcase — одна большая фича на весь экран.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-32 (много воздуха)
- Layout: grid grid-cols-1 lg:grid-cols-2 gap-12 items-center max-w-7xl mx-auto
- Сторона текста:
  - Label: text-sm uppercase tracking-widest text-[ACCENT] font-medium
  - Заголовок: text-3xl lg:text-4xl font-bold mt-2
  - Описание: text-lg text-[MUTED] mt-4 leading-relaxed
  - Буллеты (опционально): flex flex-col gap-3 mt-6
    Каждый: flex items-start gap-3
    — Иконка ✓ в круге (w-6 h-6 rounded-full bg-green-100 text-green-600)
    — Текст: text-sm
- Сторона визуала:
  - Скриншот/демо продукта
  - rounded-2xl shadow-2xl overflow-hidden
  - Или: browser mockup (rounded-xl border, "кнопки" сверху)
- Для нескольких фич: чередуй порядок (текст-лево → текст-право)
  lg:order-last на каждой второй
Feature: [ОПИСАНИЕ ФИЧИ + ВИЗУАЛ]
```

### Секция: Alternating Rows (Zigzag)

```
Создай секцию с чередующимися строками (zigzag).
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28 space-y-20 lg:space-y-32
- Каждая строка: grid grid-cols-1 lg:grid-cols-2 gap-12 items-center
  - Нечётные: текст слева, визуал справа
  - Чётные: визуал слева, текст справа (lg:order-first на визуале)
- Текстовая часть:
  - Номер/иконка: text-sm font-bold text-[ACCENT] (01, 02, 03...)
  - Заголовок: text-2xl lg:text-3xl font-bold mt-2
  - Описание: text-[MUTED] mt-4 leading-relaxed
  - Ссылка или CTA: text-[ACCENT] font-medium mt-4 inline-flex items-center gap-2
    "Подробнее →"
- Визуальная часть:
  - Изображение / скриншот / иллюстрация
  - rounded-2xl overflow-hidden shadow-lg
Строки: [МАССИВ: { number, title, description, image }]
```

### Секция: Comparison Table

```
Создай таблицу сравнения.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28
- Заголовок по центру
- Таблица: max-w-4xl mx-auto
- Desktop: html <table> или grid
  - Шапка: bg-[SECONDARY] text-sm font-medium text-[MUTED] uppercase
  - Строки: border-b border-[BORDER] py-4
  - Чередование: even:bg-[SECONDARY]/50
  - Ячейки "Да": ✓ text-green-600 font-medium
  - Ячейки "Нет": ✗ text-gray-300
  - Highlight колонка (рекомендуемая): bg-[ACCENT]/5 border-2 border-[ACCENT] rounded-xl
    с badge "Популярный" сверху
- Mobile: переключение на card-based layout
  Каждый план — отдельная карточка со списком features
Данные: [МАССИВ СТРОК И КОЛОНОК]
```

### Секция: Timeline

```
Создай Timeline-секцию (хронология/шаги/roadmap).
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28
- Заголовок по центру
- Timeline layout: relative max-w-3xl mx-auto
- Вертикальная линия: absolute left-4 lg:left-1/2 top-0 bottom-0 w-px bg-[BORDER]
- Каждый элемент: relative flex gap-6 lg:gap-12 mb-12
  - Маркер: absolute left-4 lg:left-1/2 -translate-x-1/2
    w-8 h-8 rounded-full bg-[ACCENT] border-4 border-[BACKGROUND] flex items-center justify-center
    text-white text-xs font-bold (номер)
  - Контент: ml-16 lg:ml-0
    На десктопе: чётные — слева от линии (text-right pr-16), нечётные — справа (pl-16)
  - Дата/label: text-sm text-[ACCENT] font-medium
  - Заголовок: text-xl font-semibold mt-1
  - Описание: text-[MUTED] mt-2
- Анимация: каждый элемент fade-in при скролле (stagger)
Элементы: [МАССИВ: { date, title, description }]
```

### Секция: FAQ / Accordion

```
Создай FAQ-секцию с аккордеоном.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28
- Layout: grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto
  - Левая колонка: заголовок + описание + CTA ("Остались вопросы? Напишите нам")
  - Правая колонка: accordion
  - ИЛИ: всё по центру (max-w-3xl mx-auto) без grid
- Accordion (использовать Radix UI Accordion):
  - Каждый item: border-b border-[BORDER]
  - Trigger: flex justify-between items-center w-full py-5 text-left
    text-base font-medium hover:text-[ACCENT]
    Иконка справа: ChevronDown, rotate-180 при открытии (transition-transform)
  - Content: overflow-hidden
    Анимация: animate-accordion-down / animate-accordion-up
    Текст: text-[MUTED] pb-5 leading-relaxed
- Первый вопрос — открыт по умолчанию (defaultValue)
FAQ: [МАССИВ: { question, answer }]
```

---

## КОНВЕРСИЯ

### Секция: Pricing Cards

```
Создай секцию с тарифами/ценами.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28
- Заголовок по центру + toggle Месяц/Год (опционально)
- Сетка: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8 max-w-5xl mx-auto
- Каждая карточка:
  - bg-[CARD] rounded-2xl p-8 border border-[BORDER]
  - Название тарифа: text-lg font-semibold
  - Цена: flex items-baseline gap-1 mt-4
    Число: text-5xl font-bold
    Период: text-[MUTED] text-sm /месяц
  - Описание: text-sm text-[MUTED] mt-2
  - Разделитель: h-px bg-[BORDER] my-6
  - Features list: space-y-3
    Каждая: flex items-center gap-3 text-sm
    ✓ (text-green-500) + текст
  - CTA кнопка: w-full mt-8
- Выделенный тариф (рекомендуемый):
  - border-[ACCENT] border-2 (или ring-2)
  - Badge сверху: -mt-3 absolute — "Популярный" bg-[ACCENT] text-white rounded-full px-3 py-1 text-xs
  - Кнопка: bg-[ACCENT] (primary) вместо ghost
  - scale-105 или shadow-xl
Тарифы: [МАССИВ: { name, price, period, description, features, highlighted }]
```

### Секция: CTA Banner

```
Создай CTA-баннер (призыв к действию).
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28
- Баннер: max-w-5xl mx-auto rounded-2xl lg:rounded-3xl overflow-hidden
- Вариант A (gradient):
  bg-gradient-to-r from-[ACCENT] to-[ACCENT_DARK] p-12 lg:p-16 text-white text-center
- Вариант B (с фоновым паттерном):
  bg-[ACCENT] relative (+ pattern/dots/grid поверх)
- Вариант C (карточка):
  bg-[CARD] border border-[BORDER] p-12 text-center
- Контент:
  - Заголовок: text-3xl lg:text-4xl font-bold
  - Подзаголовок: text-lg opacity-80 mt-4 max-w-xl mx-auto
  - CTA: flex flex-col sm:flex-row gap-4 justify-center mt-8
    Primary + Secondary кнопки
  - Social proof: mt-6 — "Уже 10,000+ студентов" или аватарки
Контент: [ТЕКСТ CTA — заголовок, описание, кнопка]
```

### Секция: Lead Capture (Email)

```
Создай секцию сбора email-адресов.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-16 lg:py-24 bg-[SECONDARY]
- Контент: text-center max-w-xl mx-auto px-4
- Заголовок: text-2xl lg:text-3xl font-bold
- Описание: text-[MUTED] mt-2
- Форма: flex flex-col sm:flex-row gap-3 mt-8
  - Input: flex-1 px-4 py-3 rounded-lg border border-[BORDER] bg-[BACKGROUND]
    placeholder="Ваш email" type="email"
    focus:ring-2 focus:ring-[ACCENT] focus:border-transparent outline-none
  - Кнопка: [СТИЛЬ КНОПКИ] px-6 py-3 whitespace-nowrap
- Под формой: text-xs text-[MUTED] mt-3
  "Никакого спама. Отписка в один клик."
- Состояния: loading (spinner в кнопке), success (✓ Вы подписаны!), error
- Валидация: email format, required
```

### Секция: Countdown / Urgency

```
Создай секцию с таймером обратного отсчёта.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-12 bg-[ACCENT] text-white (или bg-[CARD] для мягкого варианта)
- Layout: text-center max-w-3xl mx-auto
- Заголовок: text-xl lg:text-2xl font-bold
  "Специальная цена заканчивается через:"
- Таймер: flex justify-center gap-4 mt-6
  Каждый блок: flex flex-col items-center
  - Число: text-4xl lg:text-5xl font-bold font-mono
    bg-white/10 rounded-lg px-4 py-3 min-w-[5rem]
  - Label: text-xs uppercase mt-2 opacity-80 (дни / часы / мин / сек)
- Под таймером: CTA кнопка mt-8
- Логика: useState + useEffect с setInterval(1000ms)
  Дата окончания: [TARGET_DATE]
  При достижении 0: показать "Акция завершена" или обновить цену
```

---

## КОНТЕНТ-СЕКЦИИ

### Секция: Blog / Articles Grid

```
Создай секцию с карточками статей/блога.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28
- Заголовок секции + ссылка "Все статьи →"
- Сетка: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8
- Первая карточка (featured): md:col-span-2 lg:col-span-2
  Горизонтальная layout: grid grid-cols-2, изображение слева, текст справа
- Остальные карточки: вертикальные
  - Изображение: aspect-video rounded-t-xl overflow-hidden
    <img className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" />
  - Контент: p-6
    - Категория: text-xs uppercase tracking-wide text-[ACCENT] font-medium
    - Заголовок: text-lg font-semibold mt-2 line-clamp-2 group-hover:text-[ACCENT] transition-colors
    - Excerpt: text-sm text-[MUTED] mt-2 line-clamp-3
    - Мета: flex items-center gap-4 mt-4 text-xs text-[MUTED]
      Дата + "5 мин чтения"
Статьи: [МАССИВ: { image, category, title, excerpt, date, readTime }]
```

### Секция: Video Gallery

```
Создай секцию с видео-галереей.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: py-20 lg:py-28
- Заголовок секции
- Layout: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6
- Каждое видео:
  - group relative rounded-xl overflow-hidden cursor-pointer
  - Превью: aspect-video bg-gray-100
    <img src="[THUMBNAIL]" className="w-full h-full object-cover" />
  - Play button overlay: absolute inset-0 flex items-center justify-center
    bg-black/0 group-hover:bg-black/30 transition-colors
    Кнопка: w-16 h-16 rounded-full bg-white/90 flex items-center justify-center
    scale-0 group-hover:scale-100 transition-transform
    ▶ иконка
  - Под превью: p-4
    - Заголовок: text-sm font-medium
    - Длительность: text-xs text-[MUTED]
- Клик: открывает модалку с YouTube embed
  или расширяет карточку
Видео: [МАССИВ: { thumbnail, title, duration, youtubeId }]
```

### Секция: Team / About

```
Создай секцию "Команда" или "Об авторе".
Стиль: [ВЫБРАННЫЙ СТИЛЬ]

Вариант A — команда (несколько человек):
- Сетка: grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8
- Каждый: text-center
  - Фото: w-32 h-32 rounded-full mx-auto overflow-hidden
    (placeholder: bg-[ACCENT]/10 flex items-center justify-center text-3xl [INITIALS])
  - Имя: font-semibold mt-4
  - Роль: text-sm text-[MUTED]
  - Соцсети: flex justify-center gap-3 mt-3 (мелкие иконки)

Вариант B — один автор (детально):
- Grid: grid-cols-1 lg:grid-cols-2 gap-12 items-center
- Фото: rounded-2xl, большое (400x500)
- Текст: история автора
  - Имя: text-3xl font-bold
  - Роль/титул: text-[ACCENT] font-medium
  - Био: space-y-4, 2-3 параграфа
  - Достижения: flex flex-wrap gap-4 mt-6 (badges)
  - Соцсети: flex gap-4 mt-6
Контент: [ДАННЫЕ О КОМАНДЕ/АВТОРЕ]
```

---

## FOOTER

### Секция: Footer — Rich

```
Создай rich footer с несколькими колонками.
Стиль: [ВЫБРАННЫЙ СТИЛЬ]
Структура:
- Контейнер: bg-[DARK_BG] text-[LIGHT_TEXT] pt-16 pb-8
- Верхняя часть: max-w-7xl mx-auto px-4 grid grid-cols-2 md:grid-cols-4 gap-8
  - Колонка 1 (бренд): col-span-2 md:col-span-1
    - Лого: text-xl font-bold
    - Описание: text-sm opacity-70 mt-4 max-w-xs
    - Соцсети: flex gap-4 mt-6 (иконки: Telegram, Instagram, YouTube, etc.)
  - Колонки 2-4 (ссылки):
    - Заголовок: text-sm font-semibold uppercase tracking-wider mb-4
    - Ссылки: flex flex-col gap-2
      Каждая: text-sm opacity-70 hover:opacity-100 transition-opacity
- Разделитель: h-px bg-white/10 my-8
- Нижняя часть: flex flex-col md:flex-row justify-between items-center gap-4
  - Copyright: text-xs opacity-50
  - Правовые ссылки: flex gap-6 text-xs opacity-50
    "Политика конфиденциальности" | "Условия использования"
Ссылки: [СТРУКТУРА КОЛОНОК]
```

### Секция: Footer — Minimal

```
Создай минималистичный footer.
Структура:
- Контейнер: py-8 border-t border-[BORDER]
- Внутри: max-w-7xl mx-auto px-4 flex flex-col md:flex-row justify-between items-center gap-4
- Слева: text-sm text-[MUTED]
  "© 2026 [BRAND]. Все права защищены."
- Центр (опционально): flex gap-6 text-sm text-[MUTED]
  Ссылки на key pages
- Справа: flex gap-4
  Иконки соцсетей (w-5 h-5 text-[MUTED] hover:text-[FOREGROUND])
```

---

# ЧАСТЬ 3: ПРАВИЛА СМЕШИВАНИЯ СТИЛЕЙ

## 3 якоря единства

Эти три вещи должны быть ОДИНАКОВЫМИ на всём сайте, независимо от стилей секций:

### 1. Цветовая палитра (задать один раз)

```
Определи цветовую палитру для всего сайта:
- background: [#FFFFFF или кремовый]     — основной фон
- foreground: [#111111]                   — основной текст
- muted: [#666666]                        — приглушённый текст
- accent: [#ЦВЕТ]                         — акцентный (кнопки, ссылки, выделения)
- secondary: [#F5F5F5]                    — вторичный фон (чередование секций)
- card: [#FFFFFF]                         — фон карточек
- border: [#E5E5E5]                       — границы
- destructive: [#EF4444]                  — ошибки

Используй CSS-переменные (var(--accent), var(--background), etc.)
Определи в globals.css в :root { }
```

### 2. Типографика (задать один раз)

```
Определи типографическую систему:
- Основной шрифт: [Inter / Plus Jakarta Sans / DM Sans]
- Акцентный шрифт (опционально): [serif для заголовков, если Editorial]
- Шкала размеров (rem, строго):
  - text-xs:  0.75rem (12px)   — метки, captions
  - text-sm:  0.875rem (14px)  — вторичный текст
  - text-base: 1rem (16px)     — body
  - text-lg:  1.125rem (18px)  — подзаголовки
  - text-xl:  1.25rem (20px)   — карточки
  - text-2xl: 1.5rem (24px)    — section subtitle
  - text-3xl: 1.875rem (30px)  — section title mobile
  - text-4xl: 2.25rem (36px)   — section title tablet
  - text-5xl: 3rem (48px)      — hero subtitle
  - text-6xl: 3.75rem (60px)   — hero title
  - text-7xl: 4.5rem (72px)    — hero title desktop
- Font weights: light (300), normal (400), medium (500), semibold (600), bold (700)
```

### 3. Spacing система (задать один раз)

```
Определи систему отступов (base unit = 4px):
- Внутри элементов: p-2 (8px), p-3 (12px), p-4 (16px), p-6 (24px), p-8 (32px)
- Между элементами: gap-2, gap-3, gap-4, gap-6, gap-8
- Между секциями: py-16 (mobile) → py-20 (tablet) → py-24/py-28/py-32 (desktop)
- Контейнер: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
- ПРАВИЛО: все значения кратны 4px. Никаких произвольных значений.
```

## Что можно менять между секциями

| Свойство | Пример смены |
|----------|-------------|
| Layout | Hero centered → Features bento → Pricing cards |
| Фон | bg-white → bg-secondary → bg-accent (чередование) |
| Border-radius | rounded-2xl карточки → rounded-none таблица (макс 2 варианта) |
| Плотность | Airy hero (много воздуха) → Dense bento (плотная сетка) |
| Визуальная сложность | Простой текст → Карточки с иконками → Полноэкранное фото |

## Что НЕЛЬЗЯ менять между секциями

| Свойство | Почему |
|----------|--------|
| Шрифты | Третий шрифт = визуальный хаос |
| Цвета | Новый accent = ощущение другого сайта |
| Стиль кнопок | Rounded-full + sharp = broken design system |
| Стиль теней | Мягкие + жёсткие тени в одном дизайне — конфликт |
| Стиль иконок | Filled + outlined иконки вместе — непрофессионально |

## Проверенные рецепты (полные сайты)

### Рецепт: "Modern SaaS"
```
Стиль: Corporate Clean + Bento + Minimalism
Секции по порядку:
1. Navbar (sticky, blur)
2. Hero — Centered (badge + заголовок + CTA + social proof)
3. Logo Bar (бегущая строка)
4. Bento Features (6 фич)
5. Feature Showcase × 3 (zigzag)
6. Stats Bar
7. Testimonials Cards
8. Pricing Cards (3 тарифа)
9. FAQ Accordion
10. CTA Banner
11. Footer Rich
```

### Рецепт: "Creative Agency"
```
Стиль: Editorial + Motion-first + Dark Premium
Секции по порядку:
1. Navbar (minimal, тёмный)
2. Hero — Full-screen (видео фон + bold текст)
3. Portfolio Grid (masonry, hover reveal)
4. About / Team (split-screen)
5. Services (alternating rows с анимацией)
6. Testimonials Wall
7. Stats Bar (тёмный фон)
8. CTA Banner (gradient)
9. Footer Minimal
```

### Рецепт: "Инфопродукт / Курс"
```
Стиль: Organic + Card-based + Motion-first
Секции по порядку:
1. Navbar (простой)
2. Hero — Split (автор слева, продукт справа)
3. Stats Bar (цифры: студенты, отзывы, уроки)
4. Features Grid (что внутри курса)
5. Video Gallery (превью уроков)
6. Alternating Rows (3 причины "почему работает")
7. Testimonials Carousel
8. Author Section (подробно об авторе)
9. Pricing Cards (2 тарифа)
10. FAQ
11. CTA Banner (urgency + countdown)
12. Footer Rich
```

### Рецепт: "Luxury / Premium"
```
Стиль: Dark Premium + Aurora + Glassmorphism
Секции по порядку:
1. Navbar (прозрачный, blur)
2. Hero — Immersive (aurora gradient фон)
3. Logo Bar (белые логотипы на тёмном)
4. Bento Features (glassmorphism карточки)
5. Feature Showcase (parallax)
6. Testimonials (glassmorphism карточки)
7. Pricing (тёмные карточки, accent highlight)
8. CTA (gradient mesh)
9. Footer Minimal (тёмный)
```

### Рецепт: "Indie / Playful"
```
Стиль: Neo-brutalism + Illustrated + Motion-first
Секции по порядку:
1. Navbar (яркий, с эмодзи)
2. Hero — Centered (крупный текст + иллюстрация)
3. Features Grid (яркие карточки с border-2)
4. How It Works (timeline, пронумерованный)
5. Testimonials (стикеры, разные углы)
6. Pricing (2 карточки, яркие)
7. FAQ
8. CTA Banner (яркий фон)
9. Footer (простой, дружелюбный)
```

---

# ЧАСТЬ 4: СЕТКИ И RESPONSIVE-СИСТЕМА

## Базовые сетки

### 12-колоночная (универсальная)
```
Промпт для установки базовой сетки:

Используй 12-колоночную систему для layout:
- Контейнер: max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
- Grid: grid grid-cols-12 gap-6

Типичные раскладки:
- Полная ширина: col-span-12
- Половина: col-span-6
- Треть: col-span-4
- Две трети: col-span-8
- Четверть: col-span-3
- Три четверти: col-span-9
- Асимметрия: col-span-5 + col-span-7

На мобиле: все col-span-12 (полная ширина)
```

### Bento Grid
```
Bento Grid setup:
- Базовая сетка: grid grid-cols-2 md:grid-cols-4 gap-4
- Ячейки:
  - Standard: col-span-1 row-span-1
  - Wide: col-span-2 row-span-1
  - Tall: col-span-1 row-span-2
  - Hero: col-span-2 row-span-2
  - Full width: col-span-4 row-span-1

На мобиле (grid-cols-1): все ячейки col-span-1 row-span-1
На планшете (grid-cols-2): hero остаётся col-span-2, остальные — col-span-1
```

## Responsive-стратегия

```
Mobile-first responsive система:
1. Пиши стили сначала для мобиле (без префиксов)
2. Добавляй стили для бОльших экранов:
   - sm: (640px) — большие телефоны, маленькие планшеты
   - md: (768px) — планшеты
   - lg: (1024px) — ноутбуки
   - xl: (1280px) — десктопы
   - 2xl: (1536px) — большие экраны

Правила:
- Текст: text-3xl md:text-4xl lg:text-5xl xl:text-6xl (плавное увеличение)
- Сетка: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 (постепенное добавление колонок)
- Padding: py-16 md:py-20 lg:py-24 xl:py-32 (больше воздуха на больших экранах)
- Gap: gap-4 md:gap-6 lg:gap-8
- Контейнер: px-4 sm:px-6 lg:px-8

Паттерны адаптации:
- Navbar: горизонтальное меню → бургер (md: breakpoint)
- Hero split: стакается на мобиле (grid-cols-1 → lg:grid-cols-2)
- Cards: 1 col → 2 col → 3 col
- Logo bar: wrap → горизонтальный скролл
- Footer: стакается, 1 колонка на мобиле
- Таблица: горизонтальный скролл (overflow-x-auto) или card-based
```

---

# ЧАСТЬ 5: АНИМАЦИИ И МИКРОИНТЕРАКЦИИ

## Базовый набор (Framer Motion)

```
Настрой базовые анимации для всего проекта.
Используй Framer Motion.

1. Fade-in при скролле (для любого элемента):
const fadeInUp = {
  initial: { opacity: 0, y: 30 },
  whileInView: { opacity: 1, y: 0 },
  viewport: { once: true, margin: "-50px" },
  transition: { duration: 0.6, ease: [0.22, 1, 0.36, 1] }
}

2. Staggered fade-in (для списков/сеток):
const container = {
  whileInView: { transition: { staggerChildren: 0.1 } },
  viewport: { once: true }
}
const item = {
  initial: { opacity: 0, y: 20 },
  whileInView: { opacity: 1, y: 0 },
  transition: { duration: 0.5 }
}

3. Scale hover (для карточек):
whileHover={{ y: -4, transition: { duration: 0.2 } }}

4. Smooth entrance (для hero):
initial: { opacity: 0, y: 50 }
animate: { opacity: 1, y: 0 }
transition: { duration: 0.8, delay: 0.2, ease: [0.22, 1, 0.36, 1] }

5. ОБЯЗАТЕЛЬНО: prefers-reduced-motion
import { useReducedMotion } from "framer-motion"
const prefersReduced = useReducedMotion()
// Если true — отключить все анимации (duration: 0, no transforms)
```

## Микроинтеракции

```
Добавь микроинтеракции на элементы:

Кнопки:
- Hover: brightness increase или slight scale (1.02)
- Active/Click: scale(0.98) — "нажатие"
- Focus: ring-2 ring-[ACCENT] ring-offset-2

Карточки:
- Hover: translateY(-4px) + shadow increase
- Cursor: cursor-pointer

Ссылки:
- Hover: underline-offset-4 decoration-[ACCENT]
- Или: background highlight (bg-[ACCENT]/10)

Иконки:
- Hover на parent: rotate, scale, или color change
- transition-all duration-200

Input fields:
- Focus: ring-2 ring-[ACCENT], border-color change
- Error: ring-red-500, shake animation
- Valid: ring-green-500, checkmark appears

Toggle/Switch:
- Smooth slide (transition-transform duration-200)
- Background color change при активации
```

---

# ЧАСТЬ 6: ПРОДВИНУТЫЕ ТЕХНИКИ

## Gradient-текст
```
Заголовок с gradient-текстом:
<h1 className="bg-gradient-to-r from-[COLOR_1] via-[COLOR_2] to-[COLOR_3] bg-clip-text text-transparent">
  Текст
</h1>
```

## Dot-grid фон
```
Фон с точечной сеткой:
bg-[radial-gradient(circle,#00000010_1px,transparent_1px)] bg-[size:24px_24px]

Для тёмной темы:
bg-[radial-gradient(circle,#ffffff10_1px,transparent_1px)] bg-[size:24px_24px]
```

## Gradient-border
```
Градиентная граница:
<div className="p-px rounded-xl bg-gradient-to-r from-[COLOR_1] to-[COLOR_2]">
  <div className="bg-[BACKGROUND] rounded-xl p-6">
    Контент
  </div>
</div>
```

## Glow-эффект
```
Свечение вокруг элемента:
shadow-[0_0_30px_rgba(ACCENT_RGB,0.3)]

Hover-glow:
hover:shadow-[0_0_40px_rgba(ACCENT_RGB,0.5)] transition-shadow
```

## Animated gradient фон
```
@keyframes gradient-shift {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

bg-gradient-to-r from-[C1] via-[C2] to-[C3]
bg-[length:200%_200%]
animate-[gradient-shift_6s_ease_infinite]
```

## Noise-текстура
```
Добавь noise-текстуру для "аналогового" ощущения:
- Создай noise.svg (или base64 inline)
- Overlay: absolute inset-0 bg-[url('/noise.svg')] opacity-[0.03] pointer-events-none
- Работает на любом фоне, добавляет тактильность
```

## Marquee (бегущая строка)
```
Бесконечная бегущая строка:
<div className="overflow-hidden">
  <div className="flex gap-8 animate-[marquee_30s_linear_infinite]">
    {[...items, ...items].map(item => (
      <div className="flex-shrink-0">{item}</div>
    ))}
  </div>
</div>

@keyframes marquee {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}

hover: animation-play-state: paused
```

## Scroll-triggered counters
```
Анимированные числа (CountUp при скролле):
Используй Framer Motion useInView + useMotionValue + useTransform:

function CountUp({ target, duration = 2 }) {
  const ref = useRef(null)
  const isInView = useInView(ref, { once: true })
  const count = useMotionValue(0)
  const rounded = useTransform(count, v => Math.round(v))

  useEffect(() => {
    if (isInView) {
      animate(count, target, { duration })
    }
  }, [isInView])

  return <motion.span ref={ref}>{rounded}</motion.span>
}
```

---

# ЧАСТЬ 7: ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ

## Универсальный промпт для финальной проверки

```
Проверь компонент/страницу по чеклисту:

Визуал:
□ Цвета соответствуют палитре (нет случайных цветов)
□ Типографика по шкале (нет произвольных размеров)
□ Spacing кратен 4px
□ Контраст текста ≥ 4.5:1 (WCAG AA)
□ Hover-эффекты на всех интерактивных элементах

Responsive:
□ Mobile (375px) — читаемо, нет горизонтального скролла
□ Tablet (768px) — адекватная раскладка
□ Desktop (1280px) — используется пространство
□ Wide (1920px) — контент не растягивается (max-width)

Доступность:
□ Все img имеют alt
□ Все кнопки-иконки имеют aria-label
□ Semantic HTML (header, main, section, nav, footer)
□ Keyboard navigation работает (Tab, Enter, Escape)
□ prefers-reduced-motion обрабатывается
□ Focus-visible стили на интерактивных элементах

Производительность:
□ Изображения: next/image с sizes и priority (для hero)
□ Шрифты: next/font с display: swap
□ Анимации: transform и opacity (не width, height, top, left)
□ will-change только где нужно (не на всём)
□ Lazy loading для below-the-fold контента

Код:
□ TypeScript типы (нет any)
□ Нет console.log
□ key на всех .map() элементах
□ Нет unused imports
□ Компонент экспортируется правильно
```

---

# ЧАСТЬ 8: БЫСТРЫЙ СТАРТ — СОБЕРИ САЙТ ЗА 1 ПРОМПТ

Если хочешь собрать целую страницу за один заход — используй этот мега-промпт:

```
Создай лендинг-страницу для [ПРОДУКТ/СЕРВИС].

Стек: React 19, Next.js 16, TypeScript, Tailwind CSS 4, Framer Motion.

Дизайн:
- Стиль: [РЕЦЕПТ ИЗ ЧАСТИ 3 — например "Modern SaaS"]
- Палитра: bg [#ЦВЕТ], text [#ЦВЕТ], accent [#ЦВЕТ]
- Шрифт: [Inter / DM Sans / etc.]
- Border-radius кнопок: [rounded-full / rounded-lg / rounded-none]

Секции (сверху вниз):
1. [ТИП СЕКЦИИ] — [краткое описание контента]
2. [ТИП СЕКЦИИ] — [краткое описание контента]
3. ...

Контент:
- Заголовок hero: "[ТЕКСТ]"
- Подзаголовок: "[ТЕКСТ]"
- Features: [СПИСОК]
- Testimonials: [СПИСОК]
- Pricing: [ТАРИФЫ]
- FAQ: [ВОПРОСЫ]
- CTA: "[ТЕКСТ ПРИЗЫВА]"

Требования:
- Mobile-first responsive
- Все анимации через Framer Motion (fade-in при скролле, stagger для списков)
- prefers-reduced-motion: отключить анимации
- ARIA labels на интерактивных элементах
- Semantic HTML (section, nav, main, footer)
- Каждая секция — отдельный компонент
- Чередование фона между секциями (light/dark/accent)
```

---

*Документ создан: 2026-02-07*
*Формат: промпт-гайд для Claude Code / Cursor*
*Стек: React 19, Next.js 16, TypeScript, Tailwind CSS 4, Framer Motion*
